import styled from 'styled-components';

const Wrapper = styled.div`
    width: 100%;
    height: 100%;
    position: fixed;
    overflow: hidden;
    left: 0;
    top: 0;
    z-index: -1;
`;

export default Wrapper;
